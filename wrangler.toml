name = "canvas-mcp"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[vars]
CANVAS_URL = "https://your-school.instructure.com"

# CANVAS_API_KEY is stored as a secret (never committed to code).
# Set it with:  wrangler secret put CANVAS_API_KEY
